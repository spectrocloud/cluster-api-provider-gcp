apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: gcr.io/spectro-images-public/cluster-api-gcp/test/cluster-api-gcp-controller:v1alpha4
        name: manager
